
___

What are Utility classes?
- Utility classes are classes that can be re-used inside any single package.


Implementation :
i. <span style="color:rgb(255, 148, 148)">com.amos.Utils</span> (A package  called utils is created and it can contain the utility classes that can be used into any package)

ii. Different classes with different reusable methods can be created inside this package.