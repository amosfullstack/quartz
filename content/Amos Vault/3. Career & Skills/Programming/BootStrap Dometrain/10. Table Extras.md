
___

i. Make the Table have <span style="color:rgb(255, 255, 0)">stripes</span> like a zebra .
- add the <span style="color:rgb(255, 0, 255)">table-striped</span> class
ie:
```html
<table class="table table-striped">
```

ii. Make the s<span style="color:rgb(255, 255, 0)">tripes go to the rows</span> instead of going to the colums.
- add the <span style="color:rgb(255, 0, 255)">-columns </span>to the <span style="color:rgb(255, 0, 255)">table-striped</span> class ie
- add the table-striped-colums class
ie:
```html
<table class="table table-striped-colums">
```

iii. Change the <span style="color:rgb(255, 255, 0)">background</span> color of the row on <span style="color:rgb(255, 255, 0)">hover</span>
- add the <span style="color:rgb(255, 0, 255)">table-hover</span> class into the table tag
ie:
```html
<table class="table table-striped table-hover">
```

iv. Add the <span style="color:rgb(255, 255, 0)">borders</span> around the entire table
- add the <span style="color:rgb(255, 0, 255)">table-bordered</span> class into the table tag.
ie:
```html
<table class="table table-striped table-hover table-bordered">
```


v. To <span style="color:rgb(255, 255, 0)">remove</span> all the <span style="color:rgb(255, 255, 0)">borders</span> and the horizontal lines
- add the <span style="color:rgb(255, 0, 255)">table-borderless</span> class
```html
<table class="table table-striped table-hover table-borderless">
```

vi. Indicate the <span style="color:rgb(255, 255, 0)">active</span> row that we are on.
- add the <span style="color:rgb(255, 0, 255)">table-active</span> class into the table row.
- This works in the <span style="color:rgb(255, 255, 0)">tr</span> 
- The active row becomes darker, when we horver to it , it becomes slightly lighter.
ie:
```html
<tr class="table-active">
```

vii. We can make only one cell active.
- add <span style="color:rgb(255, 0, 255)">table-active</span> class into the table data
ie:
```html
<td class="table-active">
```