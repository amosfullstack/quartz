
## Overvierw
- Learning how to Update the database from the PHP website

## **Steps for UPDATE and  DELETE**
- Basically the modality of the script is the same as that of the insertion. All steps should be followed.
- Except only the query should change.
ie. 
1.  Check if the form was submitted correctly else return the user to the home page.
2. Grab the data from the form by referencing them using a name variable.
3.  Handle the error using the try and catch block for the query and submission.
4. Create the update query, create the prepared statemet, bind parameters and execute.
```php
$query = "UPDATE users SET username = :username,password = :pwd, email = :email WHERE user_id = 3;";

//Create a prepared statement and submit query
//Bind parameters here
//Execute the statement here
```

5. Same same for delete. It is the samesame modlity but different. That is to say same modality but different action is done.
```php
$query = "DELETE FROM users WHERE username = :username AND password = :pwd;";

//Create prepared statement and submit query
//Bind Parameters
//Execute Query
```